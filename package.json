{
  "name": "yarbs",
  "version": "0.1.7",
  "description": "yet another rocketbeanstv sendeplan",
  "main": "main.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/BakeRolls/yarbs.git"
  },
  "dependencies": {
    "electron-positioner": "^2.0.1",
    "http": "0.0.0",
    "menubar": "^2.0.17",
    "moment": "^2.10.6",
    "mu2": "^0.5.20",
    "mustache": "^2.1.3",
    "node-notifier": "^4.2.3",
    "sha1-hex": "^1.0.0"
  },
  "devDependencies": {
    "electron-prebuilt": "^0.30.2",
    "electron-packager": "^5.0.1"
  },
  "scripts": {
    "start": "electron .",
    "build": "rm -rf yarbs-*-x64; electron-packager . yarbs --platform=darwin,linux --arch=x64 --version=0.33.1 --ignore='yarbs-*' --icon=www/assets/icon/BohnIcon.icns --prune --asar",
    "package": "for arch in yarbs-*-x64; do zip -ry $arch.zip $arch; done"
  },
  "author": "bake",
  "license": "CC-BY-NC-4.0"
}
